#ifndef STD_COMPAT_H
#define STD_COMPAT_H

// Standard C++ headers that must be included early to avoid conflicts
#include <map>
#include <vector>
#include <string>
#include <memory>
#include <functional>
#include <algorithm>

#endif // STD_COMPAT_H
